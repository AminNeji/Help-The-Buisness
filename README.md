📋 To Import into n8n

Go to n8n → Workflows → Import from file
Upload the JSON
Add two credentials: Gmail OAuth2 + OpenAI API Key
Update the security-ops@yourcompany.com recipient in the Security Alert node
Activate — it will start polling Gmail immediately
